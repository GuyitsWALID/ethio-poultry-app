name: Staging release gate

on:
  workflow_dispatch:
    inputs:
      staging_url:
        description: HTTPS URL of the immutable staging deployment
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: staging-release
  cancel-in-progress: false

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: staging
    env:
      APP_ENVIRONMENT: staging
      APP_RELEASE: ${{ github.sha }}
      APP_BASE_URL: ${{ inputs.staging_url }}
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.STAGING_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.STAGING_SUPABASE_PUBLISHABLE_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.STAGING_SUPABASE_SERVICE_ROLE_KEY }}
      SUPABASE_PROJECT_REF: ${{ secrets.STAGING_SUPABASE_PROJECT_REF }}
      ADMIN_ACCESS_CODE: ${{ secrets.STAGING_ADMIN_ACCESS_CODE }}
      DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}
      E2E_REQUIRE_ROLE_CREDENTIALS: "true"
      E2E_CEO_EMAIL: ${{ secrets.STAGING_E2E_CEO_EMAIL }}
      E2E_CEO_PASSWORD: ${{ secrets.STAGING_E2E_CEO_PASSWORD }}
      E2E_FARM_MANAGER_EMAIL: ${{ secrets.STAGING_E2E_FARM_MANAGER_EMAIL }}
      E2E_FARM_MANAGER_PASSWORD: ${{ secrets.STAGING_E2E_FARM_MANAGER_PASSWORD }}
      E2E_SYSTEM_ADMIN_EMAIL: ${{ secrets.STAGING_E2E_SYSTEM_ADMIN_EMAIL }}
      E2E_SYSTEM_ADMIN_PASSWORD: ${{ secrets.STAGING_E2E_SYSTEM_ADMIN_PASSWORD }}
      E2E_ADMIN_ACCESS_CODE: ${{ secrets.STAGING_ADMIN_ACCESS_CODE }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.16.0
          cache: npm
      - run: npm ci
      - name: Verify locked release
        run: npm run release:verify
      - name: Verify staging database contract
        run: npm run db:verify
      - name: Verify Feed Control and Daily Record database behavior
        run: npm run test:db:integration
      - name: Install browser test runtime
        run: npx playwright install --with-deps chromium
      - name: Verify supported-role journeys and route authorization
        run: npm run test:browser
      - name: Smoke-test staging application
        shell: bash
        run: |
          set -euo pipefail
          test "$(curl --silent --show-error --location --output /dev/null --write-out '%{http_code}' "$APP_BASE_URL/auth/sign-in")" = "200"
          test "$(curl --silent --show-error --output /dev/null --write-out '%{http_code}' "$APP_BASE_URL/api/me/context")" = "401"
      - name: Record release evidence
        run: npm run release:manifest
